/*
 * EmailConfirmPage Messages
 *
 * This contains all the text for the EmailConfirmPage component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
    header: {
        id: 'boilerplate.containers.EmailConfirmPage.header',
        defaultMessage: 'Confirm your email',
    },
});
