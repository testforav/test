// import { fromJS } from 'immutable';
// import { makeSelectScopesPageDomain } from '../selectors';

// const selector = makeSelectScopesPageDomain();

describe('makeSelectScopesPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
