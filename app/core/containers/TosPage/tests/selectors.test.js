// import { fromJS } from 'immutable';
// import { makeSelectTosPageDomain } from '../selectors';

// const selector = makeSelectTosPageDomain();

describe('makeSelectTosPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
