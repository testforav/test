// import { fromJS } from 'immutable';
// import { makeSelectGoodbyePageDomain } from '../selectors';

// const selector = makeSelectGoodbyePageDomain();

describe('makeSelectGoodbyePageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
