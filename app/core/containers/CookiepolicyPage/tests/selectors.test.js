// import { fromJS } from 'immutable';
// import { makeSelectCookiepolicyPageDomain } from '../selectors';

// const selector = makeSelectCookiepolicyPageDomain();

describe('makeSelectCookiepolicyPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
