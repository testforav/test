// import { fromJS } from 'immutable';
// import { makeSelectFaqDetailPageDomain } from '../selectors';

// const selector = makeSelectFaqDetailPageDomain();

describe('makeSelectFaqDetailPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
