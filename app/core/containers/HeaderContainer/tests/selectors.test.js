// import { fromJS } from 'immutable';
// import { makeSelectHeaderContainerDomain } from '../selectors';

// const selector = makeSelectHeaderContainerDomain();

describe('makeSelectHeaderContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
