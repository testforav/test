// import { fromJS } from 'immutable';
// import { makeSelectSecurityPageDomain } from '../selectors';

// const selector = makeSelectSecurityPageDomain();

describe('makeSelectSecurityPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
