// import { fromJS } from 'immutable';
// import { makeSelectAccountPrivacyPageDomain } from '../selectors';

// const selector = makeSelectAccountPrivacyPageDomain();

describe('makeSelectAccountPrivacyPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
