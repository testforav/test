// import { fromJS } from 'immutable';
// import { makeSelectFaqPageDomain } from '../selectors';

// const selector = makeSelectFaqPageDomain();

describe('makeSelectFaqPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
