// import { fromJS } from 'immutable';
// import { makeSelectGaConfirmPageDomain } from '../selectors';

// const selector = makeSelectGaConfirmPageDomain();

describe('makeSelectGaConfirmPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
