// import { fromJS } from 'immutable';
// import { makeSelectAccountPageDomain } from '../selectors';

// const selector = makeSelectAccountPageDomain();

describe('makeSelectAccountPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
