// import { fromJS } from 'immutable';
// import { makeSelectPasswordResetPageDomain } from '../selectors';

// const selector = makeSelectPasswordResetPageDomain();

describe('makeSelectPasswordResetPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
