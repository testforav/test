// import { fromJS } from 'immutable';
// import { makeSelectPrivacypolicyPageDomain } from '../selectors';

// const selector = makeSelectPrivacypolicyPageDomain();

describe('makeSelectPrivacypolicyPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
