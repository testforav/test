// import { fromJS } from 'immutable';
// import { makeSelectPasswordPageDomain } from '../selectors';

// const selector = makeSelectPasswordPageDomain();

describe('makeSelectPasswordPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
