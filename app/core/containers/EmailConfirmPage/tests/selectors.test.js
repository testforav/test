// import { fromJS } from 'immutable';
// import { makeSelectEmailConfirmPageDomain } from '../selectors';

// const selector = makeSelectEmailConfirmPageDomain();

describe('makeSelectEmailConfirmPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
