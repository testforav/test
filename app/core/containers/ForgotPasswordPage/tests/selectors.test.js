// import { fromJS } from 'immutable';
// import { makeSelectForgotPasswordPageDomain } from '../selectors';

// const selector = makeSelectForgotPasswordPageDomain();

describe('makeSelectForgotPasswordPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
