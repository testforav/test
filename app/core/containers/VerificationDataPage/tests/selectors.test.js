// import { fromJS } from 'immutable';
// import { makeSelectVerificationDataPageDomain } from '../selectors';

// const selector = makeSelectVerificationDataPageDomain();

describe('makeSelectVerificationDataPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
