// import { fromJS } from 'immutable';
// import { makeSelectWalletsPageDomain } from '../selectors';

// const selector = makeSelectWalletsPageDomain();

describe('makeSelectWalletsPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
