// import { fromJS } from 'immutable';
// import { makeSelectVerificationPageDomain } from '../selectors';

// const selector = makeSelectVerificationPageDomain();

describe('makeSelectVerificationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
