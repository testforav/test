// import { fromJS } from 'immutable';
// import { makeSelectTestPageDomain } from '../selectors';

// const selector = makeSelectTestPageDomain();

describe('makeSelectTestPageDomain', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(false);
    });
});
