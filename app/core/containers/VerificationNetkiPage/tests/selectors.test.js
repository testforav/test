// import { fromJS } from 'immutable';
// import { makeSelectVerificationNetkiPageDomain } from '../selectors';

// const selector = makeSelectVerificationNetkiPageDomain();

describe('makeSelectVerificationNetkiPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
