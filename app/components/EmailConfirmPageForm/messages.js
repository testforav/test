/*
 * EmailConfirmPage Messages
 *
 * This contains all the text for the EmailConfirmPage component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
    send: {
        id: 'boilerplate.containers.EmailConfirmPage.send',
        defaultMessage: 'Send',
    },
    code: {
        id: 'boilerplate.containers.EmailConfirmPage.code',
        defaultMessage: 'Code',
    },
});
